# AGENTS.md — AI 에이전트 작업 규칙

이 저장소에서 작업하는 모든 AI 코딩 에이전트(Antigravity 등)는 아래 규칙을 예외 없이 따른다.
**이 문서와 사용자 지시가 충돌하면 즉시 작업을 멈추고 사용자에게 확인받는다.**

---

## 0. 매 작업 시작 전 필수 절차

1. **계획을 먼저 보고하고 승인을 받는다.** 승인 없이 파일을 수정·삭제·커밋하지 않는다.
2. 이번 지시 **범위 밖의 파일·스타일·컴포넌트는 어떤 것도 변경하지 않는다.**
3. 작업 후 **변경/추가/삭제한 파일 전체 목록을 경로 그대로** 보고한다.
4. 지시 내용이 이 문서의 금지 항목과 충돌하면, 실행하지 말고 충돌 사실을 먼저 보고한다.

> 이 4개 조항은 사용자가 매번 적어주지 않아도 **항상 적용된다.**

---

## 1. 작업 범위

- **모든 수정은 `ai-world-nextjs/` 폴더 안에서만 한다.**
- 저장소 루트(`D:\홈페이지\톱니바꿈월드`)의 구버전 Vite 관련 파일·설정은 **승인 없이 손대지 않는다.**
  - 루트 정리·삭제·마이그레이션은 별도 승인 사항이다. "정리하는 김에", "빌드 최적화를 위해" 등의 자체 판단으로 진행 금지.
- 배포 대상 앱은 `ai-world-nextjs` (Next.js App Router) 하나뿐이다. Vercel Root Directory 설정을 임의로 바꾸지 않는다.
- 저장소 밖의 다른 폴더는 작업 대상이 아니다. 문서에 언급된 경로라도 **실제로 존재하는지 확인 없이 전제하지 않는다.** 지시받은 파일이 해당 경로에 없으면 추측해서 다른 폴더를 대신 사용하지 말고, 없다는 사실을 그대로 보고한다.

### 이 문서의 위치 (2부 동기화)

`AGENTS.md`는 아래 **두 경로에 동일한 내용으로** 존재한다.

```
톱니바꿈월드/AGENTS.md
톱니바꿈월드/ai-world-nextjs/AGENTS.md
```

이 문서를 수정할 때는 **반드시 두 파일을 함께** 갱신한다. 한쪽만 고쳐 내용이 갈리는 상태를 만들지 않는다. 두 파일의 내용이 다른 것을 발견하면 임의로 병합하지 말고 사용자에게 보고한다.

### 헷갈리기 쉬운 파일 — 삭제·수정 전 반드시 확인

저장소에 `index.html`이라는 이름의 파일이 여러 개 있다. **이름이 아니라 전체 경로로 구분한다.**

| 경로 | 정체 | 취급 |
|---|---|---|
| `ai-world-nextjs/public/cardnews-editor/index.html` | **라이브 서비스 중인 에디터 본체.** `app/cardnews/editor/page.jsx`가 `<iframe src="/cardnews-editor/index.html">`로 서빙한다. 에디터 수정 지시는 전부 이 파일 대상이다. | 유일한 수정 대상 |
| `ai-world-nextjs/CardNews/index.html` | 로컬 도구 시절 원본 백업 사본. `server.js`·`MANUAL.md`·`README.md`와 함께 있다. 빌드·배포·런타임 **참조 0건**(2026-08-05 확인). | 수정 금지. 여기를 고쳐도 사이트는 바뀌지 않는다 |

에디터 수정 지시를 받으면, 대상 파일이 위 첫 번째 경로가 맞는지 먼저 확인하고 작업한다.

---

## 2. 에디터 코드 취급 원칙

- `public/cardnews-editor/index.html`은 **이미 검증된 단일 파일 에디터**다.
- **React 컴포넌트로 포팅하거나 리팩터링하지 않는다.** "구조가 낡았다", "유지보수성 개선" 등의 이유로 재작성 금지.
- 변경은 API 연동부·버그 수정 등 **지시받은 최소 범위**로 한정한다.
- 유지해야 하는 기존 동작(임의 제거 금지):
  - 카드 5타입(표지/본문/인용/비교표/마무리)
  - `*강조*`(포인트색) / `**굵게**` 문법, `fit()` 자동 글자 축소
  - 스타일 6종(cine/mag/manga/pixel/doodle/blue), 스타일별 배경 분리 저장, 11색 팔레트 + 커스텀 컬러
  - 배경 스크림 슬라이더(카드별), 필름스트립 드래그 정렬, 카드 추가/삭제
  - JSON 내보내기/불러오기, ZIP 내보내기(1080×1350 PNG, `01.png~NN.png`)
  - localStorage 자동저장

---

## 3. 모델명 (변경 금지)

- 기본: **`claude-haiku-4-5`**
- 옵션: **`claude-sonnet-4-6`**

> **이 모델명은 실재하며 정상 동작 중이다.** 에이전트의 학습 데이터가 오래되어 "미출시 모델", "존재하지 않는 모델명", "오타로 보임"이라고 판단될 수 있으나 **오판이다.**
> 어떤 경우에도 구모델(claude-3-5-sonnet, claude-3-haiku 등)로 임의 변경하지 않는다.
> 모델명은 **상수 한 곳에서만 정의**하고 나머지는 그 상수를 참조한다. 문자열을 여러 파일에 흩뿌리지 않는다.

---

## 4. 비밀 정보 (최우선 보안 규칙)

### 절대 금지
- 비밀번호·API 키·service role 키를 **코드·주석·placeholder·기본값·에러 메시지·보고서·채팅**에 리터럴로 쓰지 않는다.
- 폴백 패턴 금지: `process.env.X || '실제값'` ← **이 형태를 만들지 않는다.** 값이 없으면 에러를 던진다.
- 비밀 값을 사용자에게 되묻거나 보고서에 출력하지 않는다. (에이전트는 비밀 값을 알 필요가 없다)

### 필수
- 모든 비밀은 **환경변수 참조만** 한다: `CARDNEWS_PUBLISH_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- 비밀번호 **대조는 서버(Route Handler)에서만** 한다. 클라이언트 JS의 비교문은 번들에 평문 노출되므로 금지.
- 사용자 API 키(BYOK)는 요청 헤더로 받아 벤더에 전달만 한다. `console.log`·DB·에러 응답·환경변수 어디에도 기록 금지.

---

## 5. 외부 입력이 저장·렌더링되는 기능 (광고·팝업·발행 등)

새 기능이 아래 중 하나라도 해당하면 **반드시** 이 구조를 따른다.

- 관리자가 값을 입력해 DB에 저장한다
- 저장된 값이 화면에 렌더링된다 (특히 HTML·이미지 URL·이동 링크)

### 필수 구조
1. **쓰기는 서버 Route Handler(service role) 경유.** 클라이언트에서 Supabase에 직접 `insert/update/upsert/delete` 금지.
2. 서버에서 **`CARDNEWS_PUBLISH_KEY` 대조** 후에만 쓰기 허용.
3. **RLS `authenticated` 조건은 관리자 제한이 아니다.** (회원가입이 열려 있으면 누구나 authenticated가 됨) 읽기는 anon select 공개, 쓰기는 서버 전용으로 막는다.
4. `dangerouslySetInnerHTML`로 렌더링되는 필드는 위 3개를 모두 만족해야 한다.
5. 외부 URL을 중계하는 프록시는 **도메인 화이트리스트** 적용 (SSRF 방지).

해당 구조를 이미 따르고 있는 참고 구현: `/api/admin/ad-slots`, `/api/cardnews/publish`

---

## 6. BYOK 구조 (변경 금지)

- 운영자 API 키를 사용하는 AI 기능을 **만들지 않는다.** 모든 AI 기능은 사용자 본인 키로만 동작한다.
- 사용자 키는 **브라우저 localStorage에만** 저장한다.
- **키 없이도 동작해야 하는 기능**(리드마그넷 — 절대 키 필수로 바꾸지 않는다):
  빠른변환(규칙 기반), 스타일 6종, 배경 셔플/업로드, Openverse 사진 검색, JSON 저장/불러오기, ZIP/PNG 내보내기
- 팀/LAN 공유 기능(M6), `server.js`, `start.bat`, `tools/*-key.txt`, Codex 경로는 **삭제된 상태다. 복원하지 않는다.**

---

## 7. Supabase 용량·비용

- 발행 이미지는 **WebP(품질 0.8, 폭 1080)** 로 변환 후 업로드한다. **PNG 원본 업로드 금지.**
  (과거 Egress 초과로 계정 정지 이력 있음 — 무료 티어 보호가 목적)
- 새 테이블·버킷·Supabase 프로젝트를 임의로 만들지 않는다. 기존 프로젝트를 재사용한다.
- 반복 폴링·불필요한 전체 조회 등 Egress를 늘리는 패턴을 추가하기 전에 보고한다.

---

## 8. 되돌리기(revert) 규칙

- "되돌려라"는 지시를 **커밋 통째 revert로 해석하지 않는다.**
- 반드시 **"어느 커밋의 어느 변경만"** 인지 범위를 확인하고, 되돌릴 파일 목록을 **먼저 보고 후 승인**받는다.
- 되돌리기로 인해 함께 사라지는 기능이 있으면 그 목록을 사전에 명시한다.

---

## 9. 장애 진단 규칙

- 로컬에서 발생한 500/에러를 **실서버 장애로 단정하지 않는다.**
- 진단 순서: ① 실서버(Vercel 배포본)에서 재현되는지 확인 → ② 환경변수 누락 여부 확인 → ③ 그 다음에 코드 의심
- 로컬 `.env`에 키가 없어서 나는 에러 때문에 **정상 동작 중인 보안 구조를 수정하지 않는다.**
- 환경변수 변경은 코드가 아니라 Vercel 대시보드 사항이며, 변경 후 **Redeploy** 필요하다는 점을 안내한다.

---

## 10. 회귀 방지 체크 (배포 전 자체 확인)

기존 라우트가 전부 이전과 동일하게 동작해야 한다. 하나라도 영향이 있으면 사전 보고한다.

`/` · `/cardnews` · `/cardnews/editor` · `/cardnews/gallery` · `/cardnews/gallery/[id]` · `/ai-news` · `/ai-recommend` · `/download` · `/homepage` · `/insights` · `/admin`

추가 확인 항목:
- 홈 Featured Projects에 카드뉴스 카드 표시 및 `/cardnews` 이동
- 광고 섹션 상단 **쿠팡 파트너스 대가성 문구 유지** (임의 삭제·이동 금지)
- 모바일 390px에서 랜딩·네비 정상 표시
- `git status`로 루트 구버전 파일 변경 여부 확인

---

## 11. UI 수정 요청 처리

- 말로만 전달된 UI 지시는 해석이 갈린다. **모호하면 추측해서 고치지 말고 스크린샷을 요청한다.**
- "카드 높이 맞춰라" 같은 지시에서 **지시 대상 외의 인접 요소(다른 배너·슬롯·섹션)를 함께 수정하지 않는다.**
- 레이아웃 변경 시 영향 범위를 먼저 보고한다.

---

## 12. 보고 형식

작업 완료 시 아래를 포함한다.

1. 변경/추가/삭제 파일 **전체 경로 목록**
2. 각 파일에서 한 일 1줄 요약
3. 이 문서의 금지 항목 중 저촉 가능성이 있었던 지점과 처리 방법
4. 검증 결과 표 (항목 / 결과 / 비고)
5. 사용자가 직접 해야 할 후속 조치 (Vercel 환경변수, Redeploy, /admin 등록 등)

**보고서에 비밀 값·키·비밀번호를 출력하지 않는다.**
